<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_cart"
        android:title="Panier"
        app:actionLayout="@layout/item_toolbar_cart_count_menu"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_settings"
        android:title="Réglages"
        android:icon="@drawable/ic_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_support"
        android:title="Service client"
        android:icon="@drawable/ic_customer_service"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_logout"
        android:title="Se déconnecter"
        android:icon="@drawable/ic_customer_service"
        app:showAsAction="never" />

</menu>